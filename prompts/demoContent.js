/**
 * 데모용 미리 정의된 샘플 텍스트
 * - 파일 업로드 없이 즉시 요약/문제 생성을 체험할 수 있도록 제공
 * - 실제 PDF에서 추출한 텍스트와 동일한 형태로 저장
 */

export const DEMO_CONTENTS = {
  /**
   * 주제: 데이터베이스 정규화
   */
  db_normalization: {
    title: '데이터베이스 정규화',
    description: '관계형 데이터베이스 설계 원칙과 정규화 과정을 설명하는 CS 기초 내용입니다.',
    text: `
데이터베이스 정규화(Database Normalization)

정규화란 관계형 데이터베이스에서 데이터 중복을 줄이고, 데이터 무결성을 향상시키기 위해 테이블을 구조적으로 분리하는 과정이다.
정규화를 통해 삽입 이상(Insertion Anomaly), 삭제 이상(Deletion Anomaly), 갱신 이상(Update Anomaly)을 방지할 수 있다.

제1정규형 (1NF, First Normal Form)
테이블의 모든 속성 값이 원자값(Atomic Value)이어야 한다. 즉, 하나의 셀에는 하나의 값만 저장되어야 하며, 반복 그룹이나 다중 값이 허용되지 않는다.
예시: 학생 테이블에서 '수강과목' 컬럼에 여러 과목을 콤마로 구분하여 저장하는 방식은 1NF를 위반한다. 이 경우 수강 테이블을 별도로 분리해야 한다.

제2정규형 (2NF, Second Normal Form)
제1정규형을 만족하면서, 기본키가 복합키인 경우 모든 비주요 속성이 기본키 전체에 완전 함수 종속(Full Functional Dependency)되어야 한다. 부분 함수 종속(Partial Dependency)이 존재하면 2NF를 위반한다.
예시: (학번, 과목코드)가 기본키인 수강 테이블에서 '학생이름'이 학번에만 종속된다면 부분 종속이 발생하므로 학생 테이블로 분리해야 한다.

제3정규형 (3NF, Third Normal Form)
제2정규형을 만족하면서, 비주요 속성이 기본키에 직접 종속(Non-Transitive Dependency)되어야 한다. 이행 함수 종속(Transitive Dependency)이 존재하면 3NF를 위반한다.
예시: 학생 테이블에서 학번 → 학과코드 → 학과명 의 관계가 있을 때, 학과명은 학번에 이행적으로 종속된다. 이 경우 학과 테이블로 분리해야 한다.

보이스-코드 정규형 (BCNF, Boyce-Codd Normal Form)
제3정규형의 강화 버전으로, 모든 결정자(Determinant)가 후보키(Candidate Key)여야 한다. 3NF를 만족하더라도 BCNF를 위반하는 경우가 존재할 수 있다.

제4정규형 (4NF)
BCNF를 만족하면서 다치 종속(Multi-Valued Dependency)이 없어야 한다. 한 속성이 다른 속성과 독립적으로 여러 값을 가질 때 별도의 테이블로 분리해야 한다.

제5정규형 (5NF)
조인 종속성(Join Dependency)을 제거한 형태로, 모든 조인 종속이 후보키에 의해 내포되어야 한다. 실무에서는 주로 3NF 또는 BCNF까지 적용하는 것이 일반적이다.

정규화의 장단점
장점으로는 데이터 중복 최소화, 저장 공간 절약, 데이터 일관성 유지, 무결성 향상이 있다.
단점으로는 테이블 분리로 인해 조인(JOIN) 연산이 증가하고 쿼리 복잡도가 높아질 수 있으며, 경우에 따라 성능 저하가 발생할 수 있다.

역정규화(De-normalization)
실무에서는 성능 최적화를 위해 의도적으로 정규화 원칙을 완화하는 역정규화를 적용하기도 한다. 읽기 작업이 잦고 성능이 중요한 시스템에서는 중복을 허용하더라도 조인 횟수를 줄이는 방식을 선택할 수 있다. 역정규화는 항상 데이터 정합성 관리 전략과 함께 고려되어야 한다.

함수 종속성(Functional Dependency)
정규화의 핵심 개념으로, 속성 X의 값이 결정되면 속성 Y의 값도 반드시 결정되는 관계를 'X → Y (Y는 X에 함수 종속)'라 한다.
완전 함수 종속은 Y가 X 전체에 종속되는 경우이며, 부분 함수 종속은 Y가 X의 일부에만 종속되는 경우다.
이행 함수 종속은 X → Y이고 Y → Z일 때 X → Z가 성립하는 경우를 말한다.
    `.trim(),
  },
};

/**
 * 사용 가능한 데모 주제 목록 반환 (텍스트 제외)
 */
export const getDemoTopics = () =>
  Object.entries(DEMO_CONTENTS).map(([key, { title, description }]) => ({
    key,
    title,
    description,
  }));